# 容器安全

容器的安全性分为两个层面：  
一个是架构层面，比如：  
1. 容器与宿主机共享 Linux 内核  
2. 用 Namespace 隔离资源的使用  
3. 通过 shim/runC 的方式来启动容器  

但作为使用者，架构层面的东西我们是无法更改的，想要做到容器安全，只能是在容器启动时控制它的内部权限。  
具体控制主要分为两个方面：  
1. 控制容器的 capabilities  
2. 让容器以非 root 用户启动



## linux capabilities

## 控制容器的 capability

## 让容器以非 root 用户启动